import React from 'react'
import { Button } from './Button.styled'

const ReturnDate: React.FC = () => {
  return (
    <React.Fragment>
      <Button type='button'>(return date)</Button>
    </React.Fragment>
  )
}

export default ReturnDate
